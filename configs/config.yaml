# Main configuration file
defaults:
  - dataset: dataset
  - model: wavegnn
  - training: training
  - _self_

# Experiment settings
experiment:
  name: "gcn_string_wave"
  seed: 75
  device: "cuda"  # "cuda" or "cpu"
  output_dir: "./outputs"
  save_dir: "./checkpoints"

# Hydra settings
hydra:
  run:
    dir: ./outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: ./multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}

# Run settings
run:
  train: true
  resume_from: null #'20251105_173641'

# Plot settings
plot:
  plot_dataset: true
  plot_train_loss: True
  load_gt: True
  look_up_every: false  # Frequency of look-up during testing (set to None to disable)

